/*https://codeforces.com/problemset/problem/281/A?mobile=false */
#include<stdio.h>
#include<string.h>
int main()
{
    char ch[100];
    fgets(ch,sizeof(ch),stdin);
    if(ch[0]>='a' && ch[0]<='z')
    {
        ch[0]-=32;
        puts(ch);
    }
    else
    puts(ch);
    
}
